spring:
  application:
    name: api-gateway-test
  cloud:
    config:
      enabled: false
    gateway:
      enabled: false  # Disable gateway for tests

server:
  port: 0

eureka:
  client:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health
